import { DbexceptionFilter } from './dbexception-filter.filter';

describe('DbexceptionFilterFilter', () => {
  it('should be defined', () => {
    expect(new DbexceptionFilter()).toBeDefined();
  });
});
